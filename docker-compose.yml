services:
  socket:
    build:
      context: ./socket
      dockerfile: Dockerfile
    container_name: campuscove-socket
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      BACKEND_API_URL: http://backend:8000/api
      VOICE_ENABLED: "true"
      VOICE_MAX_PEERS: 4
      VOICE_CONNECT_DISTANCE: 18
      VOICE_DISCONNECT_DISTANCE: 22
      VOICE_ICE_TRANSPORT_POLICY: all
      VOICE_ICE_SERVERS_JSON: "[]"
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: campus_cove
      DB_USERNAME: postgres
      DB_PASSWORD: secret
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    container_name: campuscove-db
    environment:
      POSTGRES_DB: campus_cove
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d campus_cove"]
      interval: 5s
      timeout: 5s
      retries: 5

  postgres-test:
    image: postgres:16-alpine
    container_name: campuscove-db-test
    profiles: ["test"]
    environment:
      POSTGRES_DB: campus_cove_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    tmpfs:
      - /var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d campus_cove_test"]
      interval: 2s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: campuscove-backend
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8000
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: campus_cove
      DB_USERNAME: postgres
      DB_PASSWORD: secret
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/var/www/html
      - backend_vendor:/var/www/html/vendor
    command: sh -c "composer install --no-interaction && php artisan key:generate --force && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=8000"

  backend-test:
    build:
      context: ./backend
      dockerfile: Dockerfile
    profiles: ["test"]
    environment:
      APP_ENV: testing
      APP_KEY: "base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="
      DB_CONNECTION: pgsql
      DB_HOST: postgres-test
      DB_PORT: 5432
      DB_DATABASE: campus_cove_test
      DB_USERNAME: postgres
      DB_PASSWORD: secret
    depends_on:
      postgres-test:
        condition: service_healthy
    volumes:
      - ./backend:/var/www/html
      - backend_vendor:/var/www/html/vendor
    command: sh -c "composer install --no-interaction && composer test"

  front:
    build:
      context: ./front
      dockerfile: Dockerfile
    container_name: campuscove-front
    environment:
      VITE_API_URL: http://localhost:8000/api
      VITE_SOCKET_URL: http://localhost:3000
    ports:
      - "5173:5173"
    volumes:
      - ./front:/app
      - front_node_modules:/app/node_modules
    depends_on:
      - backend
      - socket
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"

# Named volumes persist across `docker compose down` (avoid `docker compose down -v` or data is removed)
volumes:
  postgres_data:      # DB data – survives down/up
  backend_vendor:     # Composer packages – survives down/up
  front_node_modules: # npm packages – survives down/up
